repos:
- repo: https://github.com/pre-commit/pre-commit-hooks
  rev: v5.0.0
  hooks:
    - id: trailing-whitespace
    - id: end-of-file-fixer
    - id: check-yaml
    - id: check-toml
    - id: check-added-large-files
    - id: detect-private-key

- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.12.1
  hooks:
    - id: ruff
      args: [ --fix ]
    - id: ruff-format

- repo: https://github.com/codespell-project/codespell
  rev: v2.4.1
  hooks:
  - id: codespell
    args: ["--ignore-words-list=afterall"]
    exclude: >
            (?x)^(
                .*\.jsonl|
                onlineServiceJS/static/vendor/.*
            )$

- repo: https://github.com/pre-commit/mirrors-mypy
  rev: v1.16.1
  hooks:
    - id: mypy
      args: [--explicit-package-bases, --ignore-missing-imports, --follow-imports=skip]
      exclude: ^(evaluation/patch_selection)
      additional_dependencies:
        - types-PyYAML

# 提交前跑全量单测（与 Makefile 的 uv-test 一致）；需已安装 uv 且执行过 uv sync
- repo: local
  hooks:
    - id: make-uv-test
      name: make uv-test (full test suite)
      entry: make
      args: [uv-test]
      language: system
      pass_filenames: false
      always_run: true
